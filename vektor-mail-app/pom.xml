<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.vektor</groupId>
        <artifactId>vektor-mail</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>vektor-mail-app</artifactId>
    <packaging>jar</packaging>
    <name>vektor-mail-app</name>
    <description>Spring Boot application entry point for Vektor Mail</description>

    <dependencies>
        <!-- Internal modules -->
        <dependency>
            <groupId>de.vektor</groupId>
            <artifactId>vektor-mail-core</artifactId>
        </dependency>
        <dependency>
            <groupId>de.vektor</groupId>
            <artifactId>vektor-mail-osgi</artifactId>
        </dependency>

        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-actuator-autoconfigure</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
